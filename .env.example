# Server root for API calls (UI appends /api/v1). Docker EC2: http://api:8000
API_BASE_URL=http://127.0.0.1:8001

# Optional: URL shown in the dashboard (EC2 public IP + port 8001)
# PUBLIC_API_URL=http://3.111.144.98:8001
